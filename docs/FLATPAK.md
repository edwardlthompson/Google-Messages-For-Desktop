# Flathub / Flatpak

Manifest: `packaging/flatpak/com.edwardlthompson.google-messages.yml`.

This is a **recipe**, not a published Flathub app. The supported Linux program file is the GitHub Release **`.deb`**. This optional Flatpak recipe is not the install path. Runtime is Electron BaseApp + network/notifications; no proprietary SDKs.

`[HUMAN]` files the Flathub submission. Agents do not claim the app is on Flathub until that lands.
