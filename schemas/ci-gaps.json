{
  "version": 1,
  "issue_label": "ci-gap",
  "gaps": [
    {
      "id": "nix-not-required-by-ci-ok",
      "status": "accepted",
      "note": "Optional path-filtered Nix job. ci-ok must not needs nix; skipped jobs are not success."
    },
    {
      "id": "android-emulator-skip-without-sdk",
      "status": "accepted",
      "note": "/emulator and instrumented device jobs skip without ANDROID_HOME; compile-time Android tests still run."
    },
    {
      "id": "semgrep-local-skip",
      "status": "accepted",
      "note": "check-semgrep.sh SKIPs the scanner when semgrep is not on PATH; FOSS config files stay required."
    },
    {
      "id": "pr-ci-main-only",
      "status": "accepted",
      "note": "Template id retained. This child default branch is master; CI on.pull_request.branches is [master]. Feature-branch PRs use local gates until they target master."
    },
    {
      "id": "scorecard-not-enabled",
      "status": "accepted",
      "note": "OpenSSF Scorecard workflow is enabled (weekly + push to master, informational). Not a merge-blocking required check. Product release is release-desktop.yml."
    },
    {
      "id": "pages-demo-not-shipped",
      "status": "accepted",
      "note": "GitHub Pages Golden Path demo (pages.yml) is not used; this product ships Electron installers via release-desktop.yml."
    },
    {
      "id": "weekly-health-sbom-soft",
      "status": "accepted",
      "note": "weekly-health-check.yml waits for template Golden Path SBOM assets with continue-on-error; product releases are electron-builder installers via release-desktop.yml."
    }
  ]
}
